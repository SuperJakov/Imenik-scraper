{
  "name": "puppeteer-scrape",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "bun run index.ts",
    "build": "bun build index.ts --target=bun --outdir=dist --minify",
    "build:run": "bun run build && bun run dist/index.js"
  },
  "devDependencies": {
    "@types/bun": "^1.2.10"
  },
  "peerDependencies": {
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "dotenv": "^16.5.0",
    "mongodb": "^6.16.0",
    "puppeteer": "^24.7.2"
  }
}
